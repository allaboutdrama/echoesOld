$('#sounds-near-user').html("<%= j(render :partial => 'sounds_near_user') %>");
 
var coords = window.coords = <%= raw @sounds.map {|r| {latitude: r.latitude.to_f, longitude: r.longitude.to_f}}.to_json %>;
clearMarkers(coords);
addMarkers(coords);